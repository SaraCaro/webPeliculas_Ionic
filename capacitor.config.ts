import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'peliculas_ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
